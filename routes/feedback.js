const express = require('express');
const router = express.Router();

// Placeholder route for detailed feedback
router.post('/feedback', (req, res) => {
    res.send('Feedback endpoint is working!');
});

module.exports = router;
